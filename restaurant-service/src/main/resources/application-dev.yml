server:
  port: ${PORT:${SERVER_PORT:9090}}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST:dev-postgres.intellidine.local}:${DB_PORT:5433}/${DB_NAME:restaurants_db}}
    username: ${DB_USERNAME:postgres_dev}
    password: ${DB_PASSWORD:devpassword}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

logging:
  level:
    root: INFO
    com.hunger.saviour: DEBUG
